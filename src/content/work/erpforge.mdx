---
title: "ERPForge"
description: "A manufacturing ERP built in days using Claude skills and AI workflows. Testing how domain expertise and AI-driven product processes compress the path from requirements to working system."
pubDate: 2026-03-20
tags: ["AI Development", "Manufacturing", "ERP"]
featured: true
outcome: "8-module ERP live in ~3 days — March 2026"
role: "Solo Builder. Claude skills + ChatGPT architecture"
duration: "March 2026, ongoing"
learningQuestion: "Can a product consultant's workflow be codified into AI skills, so each custom ERP builds faster than the last?"
primaryTools: ["Claude Code", "Claude API"]
tools: ["Claude Code", "Claude API", "ChatGPT", "Railway", "Cloudflare"]
phases:
  - { label: "Core Platform", status: "done" }
  - { label: "BOM Rebuild via Discovery", status: "done" }
  - { label: "Cloud Deployment", status: "done" }
  - { label: "Skill Authoring", status: "active" }
  - { label: "Platform Builder", status: "planned" }
features:
  - { label: "8-module ERP", status: "done" }
  - { label: "BOM system", status: "done" }
  - { label: "Cloud deployment", status: "done" }
  - { label: "Skill authoring", status: "active" }
  - { label: "Platform builder", status: "planned" }
cover: "/images/work/ERP Forge - Menu.png"
showCoverOnPage: false
liveUrl: "https://erpforge.kyle-mcleod.io"
---

import BuildTimeline from '../../components/BuildTimeline.tsx';

<BuildTimeline client:load milestones={[
  { phase: "Core Platform", description: "Full ERP scaffold: products, quotes, orders, inventory, manufacturing, shipping, invoices", stats: [{ value: "1 day", label: "build" }], done: true },
  { phase: "BOM Rebuild via Discovery", description: "Gave Claude domain context and skills, had it interview me on workflows and BOM config, then rebuild the data model", stats: [{ value: "~4 hrs", label: "build" }], done: true },
  { phase: "Cloud Deployment", description: "Railway + Cloudflare — containerized deploy, security, DNS", stats: [{ value: "1 day", label: "build" }], done: true },
  { phase: "Skill Authoring", description: "Writing Claude skill files to codify ERP building workflows", stats: [{ value: "In progress", label: "" }], active: true },
  { phase: "Platform Builder", description: "Automate the full interview → build → iterate loop", stats: [{ value: "Planned", label: "" }] },
]} />

## The Thesis Test

The ERP is proof the process works. That was phase one.

Phase two is the harder question: can a product consultant's workflow be codified into reusable AI skills? Discovery questions, design reviews, BOM interviews, spec generation. If yes, the next custom ERP builds faster than this one, and the one after that faster still. The skills become the product. The ERP is what they produce.

## The Problem

Traditional ERPs exist because code was expensive. Vendors built lowest-common-denominator software for a business segment, then tried to configure it per customer. Big customers got the most features. Small ones got the base package or cobbled together Excel and point solutions.

At MacroFab, we built manufacturing workflows in-house because MRPs were expensive and slow to implement. The cost structure forced that tradeoff. Build it yourself or settle for software that doesn't match your operations.

What if the cost structure changed?

## The Approach

I architected the system with ChatGPT. Talked through the design on a walk, generated a `CLAUDE.md` file and an architecture doc, then used Claude to build the full platform in a day. The interesting part isn't the ERP. It's the layer on top: Claude skills for design review, discovery question templates, documentation generation. The skills *are* the product. The ERP is proof the process works.

- **`/design-review`** structured 10-section review before writing any code
- **Discovery templates** 80 questions across 12 categories for BOM and product scoping
- **`/write-docs`** auto-generates API reference, migration guides, and changelogs

The BOM rebuild is the clearest example. I gave Claude context on typical BOM structures and manufacturing processes, gave it skills around BOM information, and had it interview me about product workflows and configurations. Then it rebuilt the data model. Four hours from kickoff to a restructured, production-ready BOM system. Work that would normally take a systems integrator weeks.

## What It Does

Eight integrated modules: Products & BOM, Quotes, Orders, Inventory, Purchasing, Manufacturing, Shipping, and Invoices. The full quote-to-order-to-ship workflow runs end to end.

The BOM system is the deepest module. Recursive multi-level BOMs with revision control, alternate components, CSV import with column auto-detection, and depth-first cost rollup through the full tree.

<div class="not-prose my-8">
  <img src="/images/work/ERPForge%20-%20BOM.png" alt="ERPForge BOM management interface" class="w-full rounded-xl border border-gray-100" />
</div>

## What's Actually Hard

Cloud hosting was new territory. A full day to get Railway + Cloudflare configured with containers, DNS, and security.

That was the easy problem. The harder one is encoding a consultant's judgment. A good product consultant doesn't just ask questions, they know which questions unlock the right configuration, in what order, and how to translate the answer into an architecture decision. Turning that into skills Claude can run reliably requires getting explicit about knowledge that usually lives in someone's head. That's what skill authoring is actually about, and it's slower than writing code.

The next milestone is automating the loop: full interview, build, and iteration without manual orchestration. That's the difference between "I can build an ERP fast" and "a product consultant with this tool can deliver a custom ERP to a manufacturer efficiently."

## What This Proves (So Far)

A product leader with the right AI workflow can stand up a realistic manufacturing ERP in days. Phase one is proven.

Phase two is live now: skill authoring and building the platform layer that automates the consultant workflow. The bet is that the skills and templates used to build this ERP can build the next one faster, and that the gap between "expert needed" and "anyone with domain knowledge can" keeps closing.

